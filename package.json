{
  "name": "handfish-design",
  "version": "1.0.0",
  "private": true,
  "description": "Tooling for the handfish-design Claude Code plugin. The plugin itself ships as markdown; this manifest exists so the generator runs as ESM on every supported Node and so the drift check is runnable as `npm test`.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "regenerate": "node scripts/regenerate-canonical-api.js",
    "check": "node scripts/regenerate-canonical-api.js --check"
  }
}
